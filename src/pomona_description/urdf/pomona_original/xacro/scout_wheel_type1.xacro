<?xml version="1.0"?>
<!--
  Copyright 2026 Pravin Oli  <pravin.oli.08@gmail.com, olipravin18@gmail.com>
  Licensed under the Apache License, Version 2.0.

  Adapted from agilexrobotics/scout_ros — scout_description/urdf/scout_wheel_type1.xacro
  Original: Copyright (c) 2015 Clearpath Robotics  /  Copyright (c) 2020 Weston Robot
  License : BSD-3-Clause
-->
<robot xmlns:xacro="http://ros.org/wiki/xacro" name="scout_wheel">

  <!-- wheel_axis: spin axis in the JOINT frame. Default ("0 1 0") suits the
       unrolled left wheels. The right wheels carry a 180° roll (rpy="3.14 0 0")
       that negates Y, so they must pass the NEGATED axis ("0 -1 0") to end up
       with the SAME effective axis. If left/right effective axes differ the
       diff-drive swaps linear<->angular; flipping all of them inverts fwd/back. -->
  <xacro:macro name="scout_wheel_type1" params="wheel_prefix wheel_axis:='0 1 0' *joint_pose">
    <link name="${wheel_prefix}_wheel_link">
      <inertial>
        <mass value="3"/>
        <origin xyz="0 0 0"/>
        <inertia ixx="0.7171" ixy="0" ixz="0"
                 iyy="0.1361" iyz="0"
                 izz="0.7171"/>
      </inertial>
      <visual>
        <origin xyz="0 0 0" rpy="0 0 0"/>
        <geometry>
          <mesh filename="package://pomona_description/meshes/wheels/wheel_type1.dae" scale="${wheel_scale} ${wheel_scale} ${wheel_scale}"/>
        </geometry>
      </visual>
      <collision>
        <origin xyz="0 0 0" rpy="${M_PI/2} 0 0"/>
        <geometry>
          <cylinder length="${wheel_length}" radius="${wheel_radius}"/>
        </geometry>
      </collision>
    </link>

    <joint name="${wheel_prefix}_wheel" type="continuous">
      <parent link="base_link"/>
      <child  link="${wheel_prefix}_wheel_link"/>
      <xacro:insert_block name="joint_pose"/>
      <axis xyz="${wheel_axis}" rpy="0 0 0"/>
    </joint>

    <!-- Friction for skid-steer -->
    <gazebo reference="${wheel_prefix}_wheel_link">
      <mu1>1.0</mu1>
      <mu2>1.0</mu2>
      <kp>1e7</kp>
      <kd>1.0</kd>
      <minDepth>0.001</minDepth>
      <maxVel>1.0</maxVel>
      <fdir1>1 0 0</fdir1>
      <material>Gazebo/DarkGrey</material>
    </gazebo>
  </xacro:macro>

</robot>
