# tomato — Gazebo models (TODO)

Crop models go here as `tomato_<variant>/` dirs, each
`model.config` + `model.sdf` + `meshes/` + `textures/`. Build them with the
**crop-asset-pipeline** skill from `cad_assets_studio/tomato`. Reference as
`model://tomato_<variant>` (add this dir to GAZEBO_MODEL_PATH). See
`models/strawberry/` for the finished pattern.
