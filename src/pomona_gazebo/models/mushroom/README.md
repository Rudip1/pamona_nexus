# mushroom — Gazebo models (TODO)

Crop models go here as `mushroom_<variant>/` dirs, each
`model.config` + `model.sdf` + `meshes/` + `textures/`. Build them with the
**crop-asset-pipeline** skill from `cad_assets_studio/mushroom`. Reference as
`model://mushroom_<variant>` (add this dir to GAZEBO_MODEL_PATH). See
`models/strawberry/` for the finished pattern.
