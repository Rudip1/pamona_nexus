# maize — Gazebo models (TODO)

Crop models go here as `maize_<variant>/` dirs, each
`model.config` + `model.sdf` + `meshes/` + `textures/`. Build them with the
**crop-asset-pipeline** skill from `cad_assets_studio/maize`. Reference as
`model://maize_<variant>` (add this dir to GAZEBO_MODEL_PATH). See
`models/strawberry/` for the finished pattern.
