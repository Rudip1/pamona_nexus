# environment — shared Gazebo props (TODO)

Crop-agnostic props shared across worlds: beds (prefer a parametric `<box>` in
the world generator over a CAD mesh), ground, greenhouse frames, posts, fences.
Each as a model dir (`model.config` + `model.sdf` + `meshes/`).
